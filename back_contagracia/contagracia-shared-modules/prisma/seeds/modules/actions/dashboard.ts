import { ActionDef } from '../types';

// ===== MÓDULO 1: DASHBOARD (1 permiso) =====
export const dashboardActions: ActionDef[] = [
    { action_key: 'dashboard.view', action_name: 'Ver Dashboard', description: 'Ver dashboard principal' },
  ];
