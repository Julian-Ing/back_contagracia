-- CreateTable
CREATE TABLE "consecutive_types" (
    "type" TEXT NOT NULL,
    "default_prefix" TEXT NOT NULL,
    "description" TEXT NOT NULL,

    CONSTRAINT "consecutive_types_pkey" PRIMARY KEY ("type")
);
